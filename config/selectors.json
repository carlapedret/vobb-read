{
  "_instructions": "search_input and search_submit calibrated 2026-09-12 against the live site. new_session_button and result_link are left null -- new_session_button because we haven't seen the stale-session interstitial live yet (the built-in text-match heuristic handles it for now), and result_link because vobb_read/voebb.py picks the right result row itself (first physical/non-online-only hit) rather than using a single fixed selector -- see its module docstring. If VOEBB changes their site and things break, re-run `vobb-read explore` and update this file / voebb.py to match.",
  "search_input": "#Autosuggest",
  "search_submit": "input[name=\"$Button\"]",
  "new_session_button": null,
  "holdings_container": null,
  "result_link": null,
  "no_results_text": [
    "keine treffer",
    "0 treffer",
    "nichts gefunden",
    "keine ergebnisse",
    "ihre suche.*ergab keine"
  ]
}
